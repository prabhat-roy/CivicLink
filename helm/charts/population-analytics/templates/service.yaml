apiVersion: v1
kind: Service
metadata:
  name: {{ include "population-analytics.fullname" . }}
  labels: {{- include "population-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "population-analytics.selectorLabels" . | nindent 4 }}