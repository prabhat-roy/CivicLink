apiVersion: v1
kind: Service
metadata:
  name: {{ include "federated-query.fullname" . }}
  labels: {{- include "federated-query.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "federated-query.selectorLabels" . | nindent 4 }}