apiVersion: v1
kind: Service
metadata:
  name: {{ include "status-tracker.fullname" . }}
  labels: {{- include "status-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "status-tracker.selectorLabels" . | nindent 4 }}