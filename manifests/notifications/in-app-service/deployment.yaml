apiVersion: apps/v1
kind: Deployment
metadata:
  name: in-app-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: in-app-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: notifications
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: in-app-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: in-app-service, civiclink.io/domain: notifications }
    spec:
      serviceAccountName: in-app-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: in-app-service
          image: harbor.civiclink.internal/civiclink/in-app-service:latest
          ports: [{ containerPort: 50324, name: http }]
          envFrom: [{ configMapRef: { name: in-app-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50324 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50324 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }