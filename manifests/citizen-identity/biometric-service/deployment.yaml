apiVersion: apps/v1
kind: Deployment
metadata:
  name: biometric-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: biometric-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: citizen-identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: biometric-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: biometric-service, civiclink.io/domain: citizen-identity }
    spec:
      serviceAccountName: biometric-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: biometric-service
          image: harbor.civiclink.internal/civiclink/biometric-service:latest
          ports: [{ containerPort: 50042, name: http }]
          envFrom: [{ configMapRef: { name: biometric-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }