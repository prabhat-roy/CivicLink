apiVersion: v1
kind: Service
metadata:
  name: death-registration
  namespace: civiclink
  labels: { app.kubernetes.io/name: death-registration, civiclink.io/domain: civil-registration }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: death-registration }