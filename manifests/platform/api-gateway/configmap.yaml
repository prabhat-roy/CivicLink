apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway
  namespace: civiclink
  labels: { app.kubernetes.io/name: api-gateway, civiclink.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "50000"