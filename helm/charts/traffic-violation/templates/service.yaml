apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffic-violation.fullname" . }}
  labels: {{- include "traffic-violation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "traffic-violation.selectorLabels" . | nindent 4 }}