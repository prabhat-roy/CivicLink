# AWS overlay for permit-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-permit-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-permit-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: permit-service-aws-secrets