apiVersion: v1
kind: Service
metadata:
  name: apostille-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: apostille-service, civiclink.io/domain: document-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: apostille-service }