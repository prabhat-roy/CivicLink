# AWS overlay for assessment-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-assessment-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-assessment-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: assessment-service-aws-secrets