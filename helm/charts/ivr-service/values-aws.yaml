# AWS overlay for ivr-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-ivr-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-ivr-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: ivr-service-aws-secrets