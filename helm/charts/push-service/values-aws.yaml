# AWS overlay for push-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-push-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-push-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: push-service-aws-secrets