apiVersion: v1
kind: Service
metadata:
  name: {{ include "fir-service.fullname" . }}
  labels: {{- include "fir-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "fir-service.selectorLabels" . | nindent 4 }}