apiVersion: v1
kind: Service
metadata:
  name: license-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: license-service, civiclink.io/domain: permits-licensing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: license-service }