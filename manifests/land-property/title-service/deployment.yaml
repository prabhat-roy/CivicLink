apiVersion: apps/v1
kind: Deployment
metadata:
  name: title-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: title-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: land-property
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: title-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: title-service, civiclink.io/domain: land-property }
    spec:
      serviceAccountName: title-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: title-service
          image: harbor.civiclink.internal/civiclink/title-service:latest
          ports: [{ containerPort: 50103, name: http }]
          envFrom: [{ configMapRef: { name: title-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }