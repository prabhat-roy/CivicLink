apiVersion: v1
kind: Service
metadata:
  name: school-registration
  namespace: civiclink
  labels: { app.kubernetes.io/name: school-registration, civiclink.io/domain: education }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: school-registration }