apiVersion: v1
kind: Service
metadata:
  name: access-log-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: access-log-service, civiclink.io/domain: audit-compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50343, targetPort: 50343 }]
  selector: { app.kubernetes.io/name: access-log-service }