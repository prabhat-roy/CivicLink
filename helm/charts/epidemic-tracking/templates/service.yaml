apiVersion: v1
kind: Service
metadata:
  name: {{ include "epidemic-tracking.fullname" . }}
  labels: {{- include "epidemic-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "epidemic-tracking.selectorLabels" . | nindent 4 }}