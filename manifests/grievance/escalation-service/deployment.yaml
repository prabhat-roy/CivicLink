apiVersion: apps/v1
kind: Deployment
metadata:
  name: escalation-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: escalation-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: grievance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: escalation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: escalation-service, civiclink.io/domain: grievance }
    spec:
      serviceAccountName: escalation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: escalation-service
          image: harbor.civiclink.internal/civiclink/escalation-service:latest
          ports: [{ containerPort: 50221, name: http }]
          envFrom: [{ configMapRef: { name: escalation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }