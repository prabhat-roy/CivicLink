apiVersion: v1
kind: Service
metadata:
  name: programme-evaluation
  namespace: civiclink
  labels: { app.kubernetes.io/name: programme-evaluation, civiclink.io/domain: analytics-policy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: programme-evaluation }