# AWS overlay for disbursement-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-disbursement-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-disbursement-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: disbursement-service-aws-secrets