# AWS overlay for school-registration.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-school-registration

podAnnotations:
  iam.amazonaws.com/role: civiclink-school-registration

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: school-registration-aws-secrets