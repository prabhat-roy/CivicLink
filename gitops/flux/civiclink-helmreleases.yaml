# Flux uses HelmReleases generated by an external tool; one HelmRelease per service.
# This file references the index HelmRelease that points at the directory.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata: { name: civiclink-platform, namespace: civiclink }
spec:
  interval: 5m
  chart:
    spec:
      chart: helm/charts/api-gateway
      sourceRef: { kind: GitRepository, name: civiclink, namespace: flux-system }
  valuesFrom:
    - kind: ConfigMap
      name: civiclink-cloud-overlay
      valuesKey: cloud-values.yaml