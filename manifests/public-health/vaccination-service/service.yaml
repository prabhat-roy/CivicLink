apiVersion: v1
kind: Service
metadata:
  name: vaccination-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: vaccination-service, civiclink.io/domain: public-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: vaccination-service }