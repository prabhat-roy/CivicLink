apiVersion: apps/v1
kind: Deployment
metadata:
  name: mfa-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: mfa-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: identity-access
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: mfa-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: mfa-service, civiclink.io/domain: identity-access }
    spec:
      serviceAccountName: mfa-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: mfa-service
          image: harbor.civiclink.internal/civiclink/mfa-service:latest
          ports: [{ containerPort: 50023, name: http }]
          envFrom: [{ configMapRef: { name: mfa-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50023 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50023 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }