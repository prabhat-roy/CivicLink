apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: civiclink, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/CivicLink.git
  ref: { branch: main }