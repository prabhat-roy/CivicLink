apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: citizen-sso
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: citizen-sso }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: citizen-sso }
  template:
    metadata:
      labels: { app.kubernetes.io/name: citizen-sso }
    spec:
      containers:
        - name: citizen-sso
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50022 }]