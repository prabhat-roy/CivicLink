apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-id-wallet.fullname" . }}
  labels: {{- include "digital-id-wallet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "digital-id-wallet.selectorLabels" . | nindent 4 }}