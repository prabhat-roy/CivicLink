# AWS overlay for attestation-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-attestation-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-attestation-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: attestation-service-aws-secrets