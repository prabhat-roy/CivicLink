apiVersion: v1
kind: Service
metadata:
  name: {{ include "officer-bff.fullname" . }}
  labels: {{- include "officer-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "officer-bff.selectorLabels" . | nindent 4 }}