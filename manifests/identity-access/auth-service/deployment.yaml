apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: identity-access
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: auth-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: auth-service, civiclink.io/domain: identity-access }
    spec:
      serviceAccountName: auth-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: auth-service
          image: harbor.civiclink.internal/civiclink/auth-service:latest
          ports: [{ containerPort: 50020, name: http }]
          envFrom: [{ configMapRef: { name: auth-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50020 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50020 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }