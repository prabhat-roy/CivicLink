apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: audit-trail
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: audit-trail }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: audit-trail }
  template:
    metadata:
      labels: { app.kubernetes.io/name: audit-trail }
    spec:
      containers:
        - name: audit-trail
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50340 }]