apiVersion: v1
kind: Service
metadata:
  name: data-retention
  namespace: civiclink
  labels: { app.kubernetes.io/name: data-retention, civiclink.io/domain: audit-compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50341, targetPort: 50341 }]
  selector: { app.kubernetes.io/name: data-retention }