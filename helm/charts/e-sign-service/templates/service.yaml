apiVersion: v1
kind: Service
metadata:
  name: {{ include "e-sign-service.fullname" . }}
  labels: {{- include "e-sign-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "e-sign-service.selectorLabels" . | nindent 4 }}