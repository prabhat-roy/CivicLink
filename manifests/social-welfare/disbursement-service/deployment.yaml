apiVersion: apps/v1
kind: Deployment
metadata:
  name: disbursement-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: disbursement-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: social-welfare
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: disbursement-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: disbursement-service, civiclink.io/domain: social-welfare }
    spec:
      serviceAccountName: disbursement-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: disbursement-service
          image: harbor.civiclink.internal/civiclink/disbursement-service:latest
          ports: [{ containerPort: 50142, name: http }]
          envFrom: [{ configMapRef: { name: disbursement-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }