apiVersion: v1
kind: Service
metadata:
  name: inspection-scheduler
  namespace: civiclink
  labels: { app.kubernetes.io/name: inspection-scheduler, civiclink.io/domain: permits-licensing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50083, targetPort: 50083 }]
  selector: { app.kubernetes.io/name: inspection-scheduler }