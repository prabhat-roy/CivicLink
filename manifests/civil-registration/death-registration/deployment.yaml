apiVersion: apps/v1
kind: Deployment
metadata:
  name: death-registration
  namespace: civiclink
  labels:
    app.kubernetes.io/name: death-registration
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: civil-registration
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: death-registration } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: death-registration, civiclink.io/domain: civil-registration }
    spec:
      serviceAccountName: death-registration
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: death-registration
          image: harbor.civiclink.internal/civiclink/death-registration:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: death-registration } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }