apiVersion: v1
kind: Service
metadata:
  name: officer-bff
  namespace: civiclink
  labels: { app.kubernetes.io/name: officer-bff, civiclink.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50002, targetPort: 50002 }]
  selector: { app.kubernetes.io/name: officer-bff }