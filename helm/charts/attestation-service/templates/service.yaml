apiVersion: v1
kind: Service
metadata:
  name: {{ include "attestation-service.fullname" . }}
  labels: {{- include "attestation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "attestation-service.selectorLabels" . | nindent 4 }}