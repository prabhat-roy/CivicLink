apiVersion: v1
kind: Service
metadata:
  name: tax-filing
  namespace: civiclink
  labels: { app.kubernetes.io/name: tax-filing, civiclink.io/domain: taxation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: tax-filing }