apiVersion: v1
kind: Service
metadata:
  name: scholarship-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: scholarship-service, civiclink.io/domain: education }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50181, targetPort: 50181 }]
  selector: { app.kubernetes.io/name: scholarship-service }