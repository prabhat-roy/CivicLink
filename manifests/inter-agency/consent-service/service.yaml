apiVersion: v1
kind: Service
metadata:
  name: consent-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: consent-service, civiclink.io/domain: inter-agency }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: consent-service }