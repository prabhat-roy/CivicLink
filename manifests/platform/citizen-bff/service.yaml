apiVersion: v1
kind: Service
metadata:
  name: citizen-bff
  namespace: civiclink
  labels: { app.kubernetes.io/name: citizen-bff, civiclink.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50001, targetPort: 50001 }]
  selector: { app.kubernetes.io/name: citizen-bff }