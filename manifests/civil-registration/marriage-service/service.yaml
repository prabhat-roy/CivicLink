apiVersion: v1
kind: Service
metadata:
  name: marriage-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: marriage-service, civiclink.io/domain: civil-registration }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: marriage-service }