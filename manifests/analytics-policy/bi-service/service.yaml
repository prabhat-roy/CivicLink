apiVersion: v1
kind: Service
metadata:
  name: bi-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: bi-service, civiclink.io/domain: analytics-policy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50303, targetPort: 50303 }]
  selector: { app.kubernetes.io/name: bi-service }