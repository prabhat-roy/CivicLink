apiVersion: v1
kind: Service
metadata:
  name: {{ include "certificate-service.fullname" . }}
  labels: {{- include "certificate-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "certificate-service.selectorLabels" . | nindent 4 }}