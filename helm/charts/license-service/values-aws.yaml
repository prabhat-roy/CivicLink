# AWS overlay for license-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-license-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-license-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: license-service-aws-secrets