apiVersion: v1
kind: Service
metadata:
  name: clinic-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: clinic-service, civiclink.io/domain: public-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: clinic-service }