apiVersion: v1
kind: Service
metadata:
  name: permit-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: permit-service, civiclink.io/domain: permits-licensing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: permit-service }