apiVersion: apps/v1
kind: Deployment
metadata:
  name: treasury-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: treasury-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: payments-finance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: treasury-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: treasury-service, civiclink.io/domain: payments-finance }
    spec:
      serviceAccountName: treasury-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: treasury-service
          image: harbor.civiclink.internal/civiclink/treasury-service:latest
          ports: [{ containerPort: 50241, name: http }]
          envFrom: [{ configMapRef: { name: treasury-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }