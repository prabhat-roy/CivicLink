apiVersion: v1
kind: Service
metadata:
  name: case-management
  namespace: civiclink
  labels: { app.kubernetes.io/name: case-management, civiclink.io/domain: social-welfare }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50143, targetPort: 50143 }]
  selector: { app.kubernetes.io/name: case-management }