apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: api-key-service, civiclink.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
