apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: epidemic-tracking
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: epidemic-tracking }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: epidemic-tracking }
  template:
    metadata:
      labels: { app.kubernetes.io/name: epidemic-tracking }
    spec:
      containers:
        - name: epidemic-tracking
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50162 }]