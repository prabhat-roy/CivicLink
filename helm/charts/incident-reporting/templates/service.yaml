apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-reporting.fullname" . }}
  labels: {{- include "incident-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "incident-reporting.selectorLabels" . | nindent 4 }}