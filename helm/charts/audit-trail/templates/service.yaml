apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-trail.fullname" . }}
  labels: {{- include "audit-trail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "audit-trail.selectorLabels" . | nindent 4 }}