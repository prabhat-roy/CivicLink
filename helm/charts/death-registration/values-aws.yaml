# AWS overlay for death-registration.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-death-registration

podAnnotations:
  iam.amazonaws.com/role: civiclink-death-registration

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: death-registration-aws-secrets