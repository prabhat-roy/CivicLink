apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: name-change-service
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: name-change-service }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: name-change-service }
  template:
    metadata:
      labels: { app.kubernetes.io/name: name-change-service }
    spec:
      containers:
        - name: name-change-service
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50063 }]