apiVersion: apps/v1
kind: Deployment
metadata:
  name: attestation-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: attestation-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: document-management
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: attestation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: attestation-service, civiclink.io/domain: document-management }
    spec:
      serviceAccountName: attestation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: attestation-service
          image: harbor.civiclink.internal/civiclink/attestation-service:latest
          ports: [{ containerPort: 50262, name: http }]
          envFrom: [{ configMapRef: { name: attestation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }