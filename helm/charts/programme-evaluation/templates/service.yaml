apiVersion: v1
kind: Service
metadata:
  name: {{ include "programme-evaluation.fullname" . }}
  labels: {{- include "programme-evaluation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "programme-evaluation.selectorLabels" . | nindent 4 }}