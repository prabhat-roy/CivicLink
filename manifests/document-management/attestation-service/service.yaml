apiVersion: v1
kind: Service
metadata:
  name: attestation-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: attestation-service, civiclink.io/domain: document-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: attestation-service }