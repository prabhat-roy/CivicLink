apiVersion: v1
kind: Service
metadata:
  name: birth-registration
  namespace: civiclink
  labels: { app.kubernetes.io/name: birth-registration, civiclink.io/domain: civil-registration }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: birth-registration }