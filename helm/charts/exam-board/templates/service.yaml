apiVersion: v1
kind: Service
metadata:
  name: {{ include "exam-board.fullname" . }}
  labels: {{- include "exam-board.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "exam-board.selectorLabels" . | nindent 4 }}