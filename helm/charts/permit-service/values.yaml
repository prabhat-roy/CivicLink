# Cloud-agnostic defaults for permit-service.
# Overlay with values-{aws|gcp|azure}.yaml for cloud-specific settings.

image:
  repository: civiclink/permit-service
  tag: ""        # defaults to .Chart.AppVersion
  pullPolicy: IfNotPresent

replicaCount: 2

service:
  type: ClusterIP
  port: 50080
  targetPort: 50080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 1000m
    memory: 512Mi

probes:
  liveness:
    httpGet:
      path: /healthz
      port: 50080
    initialDelaySeconds: 15
    periodSeconds: 20
  readiness:
    httpGet:
      path: /healthz
      port: 50080
    initialDelaySeconds: 5
    periodSeconds: 10

serviceAccount:
  create: true
  annotations: {}

podLabels:
  app.kubernetes.io/name: permit-service
  app.kubernetes.io/part-of: civiclink
  shopos.io/domain: permits-licensing
  civiclink.io/owner-team: permits-licensing

podAnnotations: {}

securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]

env: []        # additional env vars
envFrom: []    # references to ConfigMaps / Secrets

config:
  LOG_LEVEL: info

# Cloud overlay placeholders (overridden in values-<cloud>.yaml)
cloud:
  provider: ""           # aws | gcp | azure
  storageClass: ""
  ingressClass: ""