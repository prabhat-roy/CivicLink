apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: geo-address-service, civiclink.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
