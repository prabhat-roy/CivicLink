# Cloud-agnostic defaults for audit-trail.
# Overlay with values-{aws|gcp|azure}.yaml for cloud-specific settings.

image:
  repository: civiclink/audit-trail
  tag: ""        # defaults to .Chart.AppVersion
  pullPolicy: IfNotPresent

replicaCount: 2

service:
  type: ClusterIP
  port: 50340
  targetPort: 50340

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 1000m
    memory: 512Mi

probes:
  liveness:
    httpGet:
      path: /healthz
      port: 50340
    initialDelaySeconds: 15
    periodSeconds: 20
  readiness:
    httpGet:
      path: /healthz
      port: 50340
    initialDelaySeconds: 5
    periodSeconds: 10

serviceAccount:
  create: true
  annotations: {}

podLabels:
  app.kubernetes.io/name: audit-trail
  app.kubernetes.io/part-of: civiclink
  shopos.io/domain: audit-compliance
  civiclink.io/owner-team: audit-compliance

podAnnotations: {}

securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]

env: []        # additional env vars
envFrom: []    # references to ConfigMaps / Secrets

config:
  LOG_LEVEL: info

# Cloud overlay placeholders (overridden in values-<cloud>.yaml)
cloud:
  provider: ""           # aws | gcp | azure
  storageClass: ""
  ingressClass: ""