# AWS overlay for refund-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-refund-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-refund-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: refund-service-aws-secrets