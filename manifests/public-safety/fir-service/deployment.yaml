apiVersion: apps/v1
kind: Deployment
metadata:
  name: fir-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: fir-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: public-safety
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fir-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fir-service, civiclink.io/domain: public-safety }
    spec:
      serviceAccountName: fir-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fir-service
          image: harbor.civiclink.internal/civiclink/fir-service:latest
          ports: [{ containerPort: 50203, name: http }]
          envFrom: [{ configMapRef: { name: fir-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50203 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50203 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }