apiVersion: v1
kind: Service
metadata:
  name: e-sign-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: e-sign-service, civiclink.io/domain: document-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: e-sign-service }