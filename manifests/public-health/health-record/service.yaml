apiVersion: v1
kind: Service
metadata:
  name: health-record
  namespace: civiclink
  labels: { app.kubernetes.io/name: health-record, civiclink.io/domain: public-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: health-record }