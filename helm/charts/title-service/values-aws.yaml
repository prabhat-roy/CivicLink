# AWS overlay for title-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-title-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-title-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: title-service-aws-secrets