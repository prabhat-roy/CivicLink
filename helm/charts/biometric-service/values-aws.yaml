# AWS overlay for biometric-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-biometric-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-biometric-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: biometric-service-aws-secrets