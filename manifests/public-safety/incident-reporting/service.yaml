apiVersion: v1
kind: Service
metadata:
  name: incident-reporting
  namespace: civiclink
  labels: { app.kubernetes.io/name: incident-reporting, civiclink.io/domain: public-safety }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: incident-reporting }