# AWS overlay for apostille-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-apostille-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-apostille-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: apostille-service-aws-secrets