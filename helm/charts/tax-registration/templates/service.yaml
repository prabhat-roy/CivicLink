apiVersion: v1
kind: Service
metadata:
  name: {{ include "tax-registration.fullname" . }}
  labels: {{- include "tax-registration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "tax-registration.selectorLabels" . | nindent 4 }}