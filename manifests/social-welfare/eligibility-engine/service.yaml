apiVersion: v1
kind: Service
metadata:
  name: eligibility-engine
  namespace: civiclink
  labels: { app.kubernetes.io/name: eligibility-engine, civiclink.io/domain: social-welfare }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: eligibility-engine }