apiVersion: v1
kind: Service
metadata:
  name: fir-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: fir-service, civiclink.io/domain: public-safety }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: fir-service }