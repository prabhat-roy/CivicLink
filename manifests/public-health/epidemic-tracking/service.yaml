apiVersion: v1
kind: Service
metadata:
  name: epidemic-tracking
  namespace: civiclink
  labels: { app.kubernetes.io/name: epidemic-tracking, civiclink.io/domain: public-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: epidemic-tracking }