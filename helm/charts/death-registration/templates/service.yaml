apiVersion: v1
kind: Service
metadata:
  name: {{ include "death-registration.fullname" . }}
  labels: {{- include "death-registration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "death-registration.selectorLabels" . | nindent 4 }}