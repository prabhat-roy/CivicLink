apiVersion: apps/v1
kind: Deployment
metadata:
  name: bi-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: bi-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: analytics-policy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: bi-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: bi-service, civiclink.io/domain: analytics-policy }
    spec:
      serviceAccountName: bi-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: bi-service
          image: harbor.civiclink.internal/civiclink/bi-service:latest
          ports: [{ containerPort: 50303, name: http }]
          envFrom: [{ configMapRef: { name: bi-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50303 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50303 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }