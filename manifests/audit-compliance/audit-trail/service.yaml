apiVersion: v1
kind: Service
metadata:
  name: audit-trail
  namespace: civiclink
  labels: { app.kubernetes.io/name: audit-trail, civiclink.io/domain: audit-compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50340, targetPort: 50340 }]
  selector: { app.kubernetes.io/name: audit-trail }