apiVersion: v1
kind: Service
metadata:
  name: {{ include "permit-service.fullname" . }}
  labels: {{- include "permit-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "permit-service.selectorLabels" . | nindent 4 }}