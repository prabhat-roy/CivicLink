apiVersion: v1
kind: Service
metadata:
  name: in-app-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: in-app-service, civiclink.io/domain: notifications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50324, targetPort: 50324 }]
  selector: { app.kubernetes.io/name: in-app-service }