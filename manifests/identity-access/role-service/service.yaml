apiVersion: v1
kind: Service
metadata:
  name: role-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: role-service, civiclink.io/domain: identity-access }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50024, targetPort: 50024 }]
  selector: { app.kubernetes.io/name: role-service }