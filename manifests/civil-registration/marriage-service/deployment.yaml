apiVersion: apps/v1
kind: Deployment
metadata:
  name: marriage-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: marriage-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: civil-registration
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: marriage-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: marriage-service, civiclink.io/domain: civil-registration }
    spec:
      serviceAccountName: marriage-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: marriage-service
          image: harbor.civiclink.internal/civiclink/marriage-service:latest
          ports: [{ containerPort: 50062, name: http }]
          envFrom: [{ configMapRef: { name: marriage-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }