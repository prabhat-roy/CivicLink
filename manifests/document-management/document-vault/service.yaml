apiVersion: v1
kind: Service
metadata:
  name: document-vault
  namespace: civiclink
  labels: { app.kubernetes.io/name: document-vault, civiclink.io/domain: document-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: document-vault }