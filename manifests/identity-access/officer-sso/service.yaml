apiVersion: v1
kind: Service
metadata:
  name: officer-sso
  namespace: civiclink
  labels: { app.kubernetes.io/name: officer-sso, civiclink.io/domain: identity-access }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50021, targetPort: 50021 }]
  selector: { app.kubernetes.io/name: officer-sso }