apiVersion: v1
kind: Service
metadata:
  name: {{ include "escalation-service.fullname" . }}
  labels: {{- include "escalation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "escalation-service.selectorLabels" . | nindent 4 }}