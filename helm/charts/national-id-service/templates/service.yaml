apiVersion: v1
kind: Service
metadata:
  name: {{ include "national-id-service.fullname" . }}
  labels: {{- include "national-id-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "national-id-service.selectorLabels" . | nindent 4 }}