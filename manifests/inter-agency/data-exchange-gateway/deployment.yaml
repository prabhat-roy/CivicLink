apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-exchange-gateway
  namespace: civiclink
  labels:
    app.kubernetes.io/name: data-exchange-gateway
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: inter-agency
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: data-exchange-gateway } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: data-exchange-gateway, civiclink.io/domain: inter-agency }
    spec:
      serviceAccountName: data-exchange-gateway
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: data-exchange-gateway
          image: harbor.civiclink.internal/civiclink/data-exchange-gateway:latest
          ports: [{ containerPort: 50281, name: http }]
          envFrom: [{ configMapRef: { name: data-exchange-gateway } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }