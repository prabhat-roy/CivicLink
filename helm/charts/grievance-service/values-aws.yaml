# AWS overlay for grievance-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-grievance-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-grievance-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: grievance-service-aws-secrets