# AWS overlay for role-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-role-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-role-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: role-service-aws-secrets