apiVersion: v1
kind: Service
metadata:
  name: escalation-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: escalation-service, civiclink.io/domain: grievance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: escalation-service }