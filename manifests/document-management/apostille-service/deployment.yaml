apiVersion: apps/v1
kind: Deployment
metadata:
  name: apostille-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: apostille-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: document-management
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: apostille-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: apostille-service, civiclink.io/domain: document-management }
    spec:
      serviceAccountName: apostille-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: apostille-service
          image: harbor.civiclink.internal/civiclink/apostille-service:latest
          ports: [{ containerPort: 50263, name: http }]
          envFrom: [{ configMapRef: { name: apostille-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50263 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50263 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }