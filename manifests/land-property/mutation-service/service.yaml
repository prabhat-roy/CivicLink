apiVersion: v1
kind: Service
metadata:
  name: mutation-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: mutation-service, civiclink.io/domain: land-property }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: mutation-service }