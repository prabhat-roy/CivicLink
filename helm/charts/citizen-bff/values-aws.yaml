# AWS overlay for citizen-bff.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-citizen-bff

podAnnotations:
  iam.amazonaws.com/role: civiclink-citizen-bff

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: citizen-bff-aws-secrets