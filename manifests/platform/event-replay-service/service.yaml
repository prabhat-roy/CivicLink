apiVersion: v1
kind: Service
metadata:
  name: event-replay-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: event-replay-service, civiclink.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51009, targetPort: 51009 }]
  selector: { app.kubernetes.io/name: event-replay-service }
