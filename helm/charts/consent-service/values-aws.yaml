# AWS overlay for consent-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-consent-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-consent-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: consent-service-aws-secrets