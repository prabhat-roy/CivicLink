apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: right-to-erasure
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: right-to-erasure }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: right-to-erasure }
  template:
    metadata:
      labels: { app.kubernetes.io/name: right-to-erasure }
    spec:
      containers:
        - name: right-to-erasure
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50342 }]