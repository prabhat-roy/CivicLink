apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-log-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: access-log-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: audit-compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: access-log-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: access-log-service, civiclink.io/domain: audit-compliance }
    spec:
      serviceAccountName: access-log-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: access-log-service
          image: harbor.civiclink.internal/civiclink/access-log-service:latest
          ports: [{ containerPort: 50343, name: http }]
          envFrom: [{ configMapRef: { name: access-log-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50343 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50343 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }