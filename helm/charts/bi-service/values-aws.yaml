# AWS overlay for bi-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-bi-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-bi-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: bi-service-aws-secrets