apiVersion: v1
kind: Service
metadata:
  name: {{ include "mutation-service.fullname" . }}
  labels: {{- include "mutation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "mutation-service.selectorLabels" . | nindent 4 }}