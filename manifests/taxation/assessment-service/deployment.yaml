apiVersion: apps/v1
kind: Deployment
metadata:
  name: assessment-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: assessment-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: taxation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: assessment-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: assessment-service, civiclink.io/domain: taxation }
    spec:
      serviceAccountName: assessment-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: assessment-service
          image: harbor.civiclink.internal/civiclink/assessment-service:latest
          ports: [{ containerPort: 50122, name: http }]
          envFrom: [{ configMapRef: { name: assessment-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }