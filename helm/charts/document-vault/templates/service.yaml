apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-vault.fullname" . }}
  labels: {{- include "document-vault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "document-vault.selectorLabels" . | nindent 4 }}