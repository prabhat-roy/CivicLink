apiVersion: v1
kind: Service
metadata:
  name: {{ include "access-log-service.fullname" . }}
  labels: {{- include "access-log-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "access-log-service.selectorLabels" . | nindent 4 }}