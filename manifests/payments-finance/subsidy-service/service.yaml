apiVersion: v1
kind: Service
metadata:
  name: subsidy-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: subsidy-service, civiclink.io/domain: payments-finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: subsidy-service }