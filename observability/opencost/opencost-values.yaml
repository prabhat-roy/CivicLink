opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: civiclink-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.civiclink.internal, paths: [{ path: /, pathType: Prefix }] }]
