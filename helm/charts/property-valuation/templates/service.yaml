apiVersion: v1
kind: Service
metadata:
  name: {{ include "property-valuation.fullname" . }}
  labels: {{- include "property-valuation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "property-valuation.selectorLabels" . | nindent 4 }}