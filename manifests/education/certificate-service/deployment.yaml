apiVersion: apps/v1
kind: Deployment
metadata:
  name: certificate-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: certificate-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: education
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: certificate-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: certificate-service, civiclink.io/domain: education }
    spec:
      serviceAccountName: certificate-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: certificate-service
          image: harbor.civiclink.internal/civiclink/certificate-service:latest
          ports: [{ containerPort: 50183, name: http }]
          envFrom: [{ configMapRef: { name: certificate-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50183 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50183 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }