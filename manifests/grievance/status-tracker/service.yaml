apiVersion: v1
kind: Service
metadata:
  name: status-tracker
  namespace: civiclink
  labels: { app.kubernetes.io/name: status-tracker, civiclink.io/domain: grievance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: status-tracker }