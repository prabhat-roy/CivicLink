apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: land-registry
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: land-registry }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: land-registry }
  template:
    metadata:
      labels: { app.kubernetes.io/name: land-registry }
    spec:
      containers:
        - name: land-registry
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50100 }]