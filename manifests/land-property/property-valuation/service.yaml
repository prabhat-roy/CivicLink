apiVersion: v1
kind: Service
metadata:
  name: property-valuation
  namespace: civiclink
  labels: { app.kubernetes.io/name: property-valuation, civiclink.io/domain: land-property }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: property-valuation }