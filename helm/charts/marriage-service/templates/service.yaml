apiVersion: v1
kind: Service
metadata:
  name: {{ include "marriage-service.fullname" . }}
  labels: {{- include "marriage-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "marriage-service.selectorLabels" . | nindent 4 }}