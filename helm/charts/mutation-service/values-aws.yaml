# AWS overlay for mutation-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-mutation-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-mutation-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: mutation-service-aws-secrets