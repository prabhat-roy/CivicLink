# AWS overlay for survey-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-survey-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-survey-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: survey-service-aws-secrets