apiVersion: apps/v1
kind: Deployment
metadata:
  name: e-sign-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: e-sign-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: document-management
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: e-sign-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: e-sign-service, civiclink.io/domain: document-management }
    spec:
      serviceAccountName: e-sign-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: e-sign-service
          image: harbor.civiclink.internal/civiclink/e-sign-service:latest
          ports: [{ containerPort: 50261, name: http }]
          envFrom: [{ configMapRef: { name: e-sign-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }