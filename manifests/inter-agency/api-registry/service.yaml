apiVersion: v1
kind: Service
metadata:
  name: api-registry
  namespace: civiclink
  labels: { app.kubernetes.io/name: api-registry, civiclink.io/domain: inter-agency }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: api-registry }