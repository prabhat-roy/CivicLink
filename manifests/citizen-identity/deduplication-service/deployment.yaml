apiVersion: apps/v1
kind: Deployment
metadata:
  name: deduplication-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: deduplication-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: citizen-identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: deduplication-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: deduplication-service, civiclink.io/domain: citizen-identity }
    spec:
      serviceAccountName: deduplication-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: deduplication-service
          image: harbor.civiclink.internal/civiclink/deduplication-service:latest
          ports: [{ containerPort: 50043, name: http }]
          envFrom: [{ configMapRef: { name: deduplication-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50043 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50043 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }