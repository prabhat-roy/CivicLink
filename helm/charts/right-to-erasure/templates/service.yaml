apiVersion: v1
kind: Service
metadata:
  name: {{ include "right-to-erasure.fullname" . }}
  labels: {{- include "right-to-erasure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "right-to-erasure.selectorLabels" . | nindent 4 }}