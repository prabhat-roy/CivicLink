apiVersion: v1
kind: Service
metadata:
  name: {{ include "name-change-service.fullname" . }}
  labels: {{- include "name-change-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "name-change-service.selectorLabels" . | nindent 4 }}