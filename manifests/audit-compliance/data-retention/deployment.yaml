apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-retention
  namespace: civiclink
  labels:
    app.kubernetes.io/name: data-retention
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: audit-compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: data-retention } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: data-retention, civiclink.io/domain: audit-compliance }
    spec:
      serviceAccountName: data-retention
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: data-retention
          image: harbor.civiclink.internal/civiclink/data-retention:latest
          ports: [{ containerPort: 50341, name: http }]
          envFrom: [{ configMapRef: { name: data-retention } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50341 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50341 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }