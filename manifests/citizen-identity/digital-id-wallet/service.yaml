apiVersion: v1
kind: Service
metadata:
  name: digital-id-wallet
  namespace: civiclink
  labels: { app.kubernetes.io/name: digital-id-wallet, civiclink.io/domain: citizen-identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: digital-id-wallet }