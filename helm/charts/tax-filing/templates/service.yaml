apiVersion: v1
kind: Service
metadata:
  name: {{ include "tax-filing.fullname" . }}
  labels: {{- include "tax-filing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "tax-filing.selectorLabels" . | nindent 4 }}