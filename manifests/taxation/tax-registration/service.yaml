apiVersion: v1
kind: Service
metadata:
  name: tax-registration
  namespace: civiclink
  labels: { app.kubernetes.io/name: tax-registration, civiclink.io/domain: taxation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: tax-registration }