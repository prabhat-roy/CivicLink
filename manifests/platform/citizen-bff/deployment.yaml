apiVersion: apps/v1
kind: Deployment
metadata:
  name: citizen-bff
  namespace: civiclink
  labels:
    app.kubernetes.io/name: citizen-bff
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: citizen-bff } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: citizen-bff, civiclink.io/domain: platform }
    spec:
      serviceAccountName: citizen-bff
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: citizen-bff
          image: harbor.civiclink.internal/civiclink/citizen-bff:latest
          ports: [{ containerPort: 50001, name: http }]
          envFrom: [{ configMapRef: { name: citizen-bff } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50001 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50001 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }