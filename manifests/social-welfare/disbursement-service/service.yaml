apiVersion: v1
kind: Service
metadata:
  name: disbursement-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: disbursement-service, civiclink.io/domain: social-welfare }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: disbursement-service }