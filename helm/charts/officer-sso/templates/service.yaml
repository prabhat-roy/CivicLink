apiVersion: v1
kind: Service
metadata:
  name: {{ include "officer-sso.fullname" . }}
  labels: {{- include "officer-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "officer-sso.selectorLabels" . | nindent 4 }}