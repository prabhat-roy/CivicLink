apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinic-service.fullname" . }}
  labels: {{- include "clinic-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "clinic-service.selectorLabels" . | nindent 4 }}