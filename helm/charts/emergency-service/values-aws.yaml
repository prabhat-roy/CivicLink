# AWS overlay for emergency-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-emergency-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-emergency-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: emergency-service-aws-secrets