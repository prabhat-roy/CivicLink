apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: eligibility-engine
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: eligibility-engine }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: eligibility-engine }
  template:
    metadata:
      labels: { app.kubernetes.io/name: eligibility-engine }
    spec:
      containers:
        - name: eligibility-engine
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50141 }]