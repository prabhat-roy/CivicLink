apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaccination-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: vaccination-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: public-health
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: vaccination-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: vaccination-service, civiclink.io/domain: public-health }
    spec:
      serviceAccountName: vaccination-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: vaccination-service
          image: harbor.civiclink.internal/civiclink/vaccination-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: vaccination-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }