# AWS overlay for mfa-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-mfa-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-mfa-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: mfa-service-aws-secrets