apiVersion: apps/v1
kind: Deployment
metadata:
  name: clinic-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: clinic-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: public-health
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: clinic-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: clinic-service, civiclink.io/domain: public-health }
    spec:
      serviceAccountName: clinic-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: clinic-service
          image: harbor.civiclink.internal/civiclink/clinic-service:latest
          ports: [{ containerPort: 50163, name: http }]
          envFrom: [{ configMapRef: { name: clinic-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }