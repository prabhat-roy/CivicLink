apiVersion: apps/v1
kind: Deployment
metadata:
  name: role-service
  namespace: civiclink
  labels:
    app.kubernetes.io/name: role-service
    app.kubernetes.io/part-of: civiclink
    civiclink.io/domain: identity-access
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: role-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: role-service, civiclink.io/domain: identity-access }
    spec:
      serviceAccountName: role-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: role-service
          image: harbor.civiclink.internal/civiclink/role-service:latest
          ports: [{ containerPort: 50024, name: http }]
          envFrom: [{ configMapRef: { name: role-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50024 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50024 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }