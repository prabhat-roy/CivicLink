apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: civiclink-vault-issuer }
spec:
  vault:
    server: https://vault.civiclink.internal
    path: pki/sign/civiclink-mesh
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
        secretRef: { name: cert-manager-vault-token, key: token }