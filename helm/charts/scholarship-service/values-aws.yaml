# AWS overlay for scholarship-service.
cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/civiclink-scholarship-service

podAnnotations:
  iam.amazonaws.com/role: civiclink-scholarship-service

# Pulled by AWS Secrets Manager CSI driver
envFrom:
  - secretRef:
      name: scholarship-service-aws-secrets