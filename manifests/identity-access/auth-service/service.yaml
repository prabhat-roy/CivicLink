apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: auth-service, civiclink.io/domain: identity-access }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50020, targetPort: 50020 }]
  selector: { app.kubernetes.io/name: auth-service }