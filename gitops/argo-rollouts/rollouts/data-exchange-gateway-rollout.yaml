apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: data-exchange-gateway
  namespace: civiclink
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: civiclink-canary-analysis }]
            args:
              - { name: service-name, value: data-exchange-gateway }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: data-exchange-gateway }
  template:
    metadata:
      labels: { app.kubernetes.io/name: data-exchange-gateway }
    spec:
      containers:
        - name: data-exchange-gateway
          image: harbor.civiclink.internal/civiclink/
          ports: [{ containerPort: 50281 }]