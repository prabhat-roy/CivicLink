apiVersion: v1
kind: Service
metadata:
  name: assessment-service
  namespace: civiclink
  labels: { app.kubernetes.io/name: assessment-service, civiclink.io/domain: taxation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: assessment-service }