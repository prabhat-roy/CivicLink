apiVersion: v1
kind: Service
metadata:
  name: {{ include "inspection-scheduler.fullname" . }}
  labels: {{- include "inspection-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "inspection-scheduler.selectorLabels" . | nindent 4 }}